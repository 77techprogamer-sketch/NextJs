import React from 'react';

/**
 * Honeypot trap removed — was causing GSC 404 errors.
 * Bot protection is handled entirely by middleware.ts now.
 */
export const HoneypotTrap: React.FC = () => {
    return null;
};

export default HoneypotTrap;
